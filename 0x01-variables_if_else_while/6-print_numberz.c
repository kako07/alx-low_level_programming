#include <stdio.h>

/**
 * main - prints all single numbers of base 10
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	a = 0;
	while (a < 10)
	{
		putchar(a + '0');
		n++
	}
	putchar('\n');
	return (0);
}
