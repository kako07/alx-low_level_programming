#include "main.h"

/**
 * add - prints sum of two intergers
 * @a  : parameter to check
 * Return: Always 0 (success)
 */

int add(int a, int b)
{
	int sum = a + b;

	_putchar(sum + '0');
	return (sum);
}
