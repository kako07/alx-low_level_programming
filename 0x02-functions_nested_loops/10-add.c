#include "main.h"

/**
 * add - prints sum of two intergers
 * @a  : parameter to check
 * @b : parameter to check
 * Return: Always 0 (success)
 */

int add(int a, int b)
{
	int sum = a + b;
	return (sum);
}
