#include "main.h"

/**
 * _isupper - checks for uppercase characters
 * @c : parameter to check
 * Return: Always 0 or 1
 */

int _isupper(int c)
{
	if (c >= 'A' || a <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
